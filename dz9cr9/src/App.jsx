import './App.css';
import PersonComponent from './components/PersonComponent'

const App = () => {
  return (
    <div>
      <PersonComponent
        render={
          (person) => (
            Object.entries(person).filter();

      return <div></div>
      )
      }
      />
    </div>
  );
};

export default App;